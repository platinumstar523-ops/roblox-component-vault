# Serializer property coverage

Property coverage starts intentionally conservative. The serializer reads a curated set of stable UI properties for supported classes, and uses protected reads so unavailable or changed properties become warnings instead of hard failures.

Coverage will expand based on real-world components and bug reports.
