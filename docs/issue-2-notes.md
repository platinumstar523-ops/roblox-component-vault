# Serializer milestone

The serializer milestone converts the selected UI hierarchy into Component Vault's versioned plain-data schema. It deliberately does not persist or reconstruct the component yet; those are separate follow-up milestones.
