# Component tags

Saved components can have optional comma-separated tags. Tags are normalized, deduplicated case-insensitively, limited to 12 per component, and searchable from the library search box.
