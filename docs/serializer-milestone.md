# Serializer milestone

The current milestone turns a supported selected Roblox `GuiObject` into Component Vault's versioned plain-data representation. The plugin reports captured instance/property counts and explicit warnings for unsupported descendants or properties.

Persistence and reconstruction are intentionally separate follow-up milestones.
