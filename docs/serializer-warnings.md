# Serializer warnings

Warnings are part of the serialized result so unsupported descendants or properties are visible during development. The serializer prefers an explicit partial result with warnings over silently dropping data.
