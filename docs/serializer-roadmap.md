# Serializer roadmap

Current: serialize supported UI hierarchies into schema v1.

Next: implement a rebuilder that restores serialized values as normal Roblox instances, then add local persistence and the component library workflow.
