# Plugin UI

This folder is reserved for reusable Component Vault Studio UI modules as the interface grows.

The current prototype still builds its dock widget from `src/init.server.luau`. The next refactor will move library cards, dialogs, and reusable controls here once their behavior is stable.
